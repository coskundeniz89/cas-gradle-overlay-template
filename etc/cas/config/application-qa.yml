logging:
  file: logs/login/app.log
  level:
    root: WARN

cas:
  view:
    defaultRedirectUrl: https://localhost:20000/

server:
  ssl:
    key-store: classpath:keystore/thekeystore.jks
    key-store-password: changeit
    key-password: changeit

